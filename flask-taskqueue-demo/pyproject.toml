[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flask-taskqueue"
version = "0.0.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["flask_taskqueue*"]
exclude = ["example_app*"]
